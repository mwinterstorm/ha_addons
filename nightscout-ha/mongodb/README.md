# Nightscout MongoDB

Authenticated MongoDB 8.0.32, reachable only over Home Assistant's internal app
network. Persistent database in `/data/mongodb`; cold Home Assistant backups.
Requires AVX (amd64) or ARMv8.2-A (aarch64). See Documentation before installing.
